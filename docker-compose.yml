version: '3.8'

services:
  frontend:
    build: .
    container_name: innovatech_frontend
    restart: always
    ports:
      - "80:80"
    environment:
      VITE_API_URL: ${VITE_API_URL:-}
    labels:
      - "com.innovatech.service=frontend"
